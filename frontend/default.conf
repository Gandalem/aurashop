server {
    listen 80;
    server_name localhost;

    location / {
        root   /usr/share/nginx/html;
        index  index.html index.htm;

        # 🌟 404 에러를 막아주는 React 라우팅 마법의 코드!
        try_files $uri $uri/ /index.html;
    }
}